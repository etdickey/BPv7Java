package BPv7.interfaces;

public interface NodeID {
    /* Contains a node ID, which could be in a variety of formats
Todo:: sending (needs to be asynchronous for application layer API), receiving, responding to received messages, forward packets, common objects (block/NodeID/etc)
     */

}
