package BPv7.interfaces;

public class Bundle {
    /*
    Getters and setters for all fields specified in protocol
    Flag setters (separate because named differently)
*/
    String getNetworkEncoding() { /* Returns a valid network encoding of the bundle */
       return "placeholder";
    }

}
